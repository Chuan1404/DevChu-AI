from django.urls import path, include
from rest_framework.routers import DefaultRouter
from . import views


# router = DefaultRouter()
# router.register("predict", views.TestModelViewAPIView.as_view(), basename="testModel")

urlpatterns = [
   # path("", views.image_classification.as_view())
]